import React from 'react';
import {
  pure, withProps, withHandlers, compose, withState,
} from 'recompose';
import './App.css';

const App = () => (
  <div id="container">
 
  </div>
);

export default enhanced(App);
